extends layout

block content
	if jumbotron
		.jumbotron
			h1= title
			p Welcome to my blog!
		.content
			.rows
				.col-md-10.col-md-offset-1
					each post in postList
						.well
							.page-header 
								h2
									a(href= post.href) #{post.title}
							p.lead #{post.postPrev}
